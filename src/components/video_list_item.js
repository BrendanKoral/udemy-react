import React from 'react';

const VideoListItem = ({video}) => {
    //Passing in an object with the property is the same as the below
    // const video = props.video;

    return <li>Video</li>
}

export default VideoListItem;